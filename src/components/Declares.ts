export enum Variant {
    Default, Success, Danger, Warning
}

export enum Style {
    Primary, Secondary, Tertiary
}

export enum InputType {
    Text, Password, Email, Number, Date, Time, DateTime, Month, Week, Url, Tel, Search
}
